<?xml version="1.0" encoding="UTF-8"?>
<page xmlns="http://jboss.com/products/seam/pages" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://jboss.com/products/seam/pages http://jboss.com/products/seam/pages-2.0.xsd">

	<param name="usuarioId" value="#{usuarioEditarControlador.idUsuario}" />
	<param name="selectedTab" value="#{usuarioEditarControlador.selectedTab}" />
	<param name="order" />
	<param name="firstResult" />
	<param name="critB1" />
	<param name="critB2" />
	<param name="critB3" />
	<param name="critB4" />
	<param name="busquedaTipo" />
	<param name="open" />

	<navigation from-action="#{usuarioEditarControlador.modificar()}">
		<rule if-outcome="verDetalles">
			<redirect
				view-id="/modConfiguracion/management/gestionarUsuarios/VerDetallesUsuario.xhtml">
				<param name="usuarioId" value="#{usuarioEditarControlador.idUsuario}" />
				<param name="order" value="#{order}" />
				<param name="firstResult" value="#{firstResult}" />
				<param name="critB1" value="#{critB1}" />
				<param name="critB2" value="#{critB2}" />
				<param name="critB3" value="#{critB3}" />
				<param name="critB4" value="#{critB4}" />
				<param name="busquedaTipo" value="#{busquedaTipo}" />
				<param name="open" value="#{open}" />
				<param name="from" value="modificar" />
			</redirect>
			<end-conversation before-redirect="true" />
		</rule>
	</navigation>

	<navigation from-action="salir">
		<redirect
			view-id="/modConfiguracion/management/gestionarUsuarios/BuscarUsuario.xhtml">
			<param name="order" value="#{order}" />
			<param name="firstResult" value="#{firstResult}" />
			<param name="critB1" value="#{critB1}" />
			<param name="critB2" value="#{critB2}" />
			<param name="critB3" value="#{critB3}" />
			<param name="critB4" value="#{critB4}" />
			<param name="busquedaTipo" value="#{busquedaTipo}" />
			<param name="open" value="#{open}" />
		</redirect>
		<end-conversation before-redirect="true" />
	</navigation>

</page>
